<article>
	<div class="info">
		<i class="fa {{category.icon}}"></i>
		<span class="category">{{category.name}}</span>
		<span class="author">{{{linkAuthor author}}}</span>
	</div>
	<h3><a href="{{url}}" target="_blank">{{title}}</a></h3>
	<div class="blurb">{{{blurb}}}</div>
</article>

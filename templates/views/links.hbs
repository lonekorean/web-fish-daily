{{#if announcement}}
	<div class="announcement">
		{{#if announcement.headline}}<strong>{{{announcement.headline}}}</strong> &mdash;{{/if}}
		{{{announcement.message}}}
	</div>
{{/if}}

{{#if links.length}}
	<div class="cards">
		{{#each links}}
			{{> card this}}
		{{/each}}
	</div>
{{else}}
	<div class="notice">
		Sorry, no links for this day.
	</div>
{{/if}}

{{#if sneakPeakInterval}}
	<div class="countdown">
		<i class="fa fa-clock-o"></i>
		Sneak peak at tomorrow's catch in <span class="time" data-interval="{{sneakPeakInterval}}"></span>.
	</div>
{{/if}}
{{#if tomorrowInterval}}
	<div class="countdown">
		<i class="fa fa-clock-o"></i>
		Tomorrow's catch arrives in <span class="time" data-interval="{{tomorrowInterval}}"></span>.
		{{#if sneakPeakLink}}Here's a sneak peak.{{/if}}
	</div>
{{/if}}
{{#if sneakPeakLink}}
	<div class="cards">
		{{> card sneakPeakLink}}
	</div>
{{/if}}
